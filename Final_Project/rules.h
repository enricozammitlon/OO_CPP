#ifndef RULES_H
#define RULES_H

#include <iostream>

namespace battle_ship {
const enum class x_axis { A, B, C, D, E, F, G, H, I, J };
const enum class y_axis { start = 1, end = 10 };
} // namespace battle_ship
#endif